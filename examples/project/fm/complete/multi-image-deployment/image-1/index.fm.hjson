{
    kind: step/empty
    description: profile combiner for image-1

    profiles: {
        *: {
            steps: {
                push: ./push
            }
            outputs: {
                image: ${steps.push.url}
            }
        }
    }
}
