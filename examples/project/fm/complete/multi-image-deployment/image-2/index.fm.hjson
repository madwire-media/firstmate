{
    kind: step/empty
    description: profile combiner for image-2

    profiles: {
        *: {
            deps: {
                push: ./default/push
            }
            outputs: {
                image: ${deps.push.url}
            }
        }

        local: {
            deps: {
                local: ./local/run
            }
            outputs: {
                image: ${deps.local.proxyImage}
                proxyId: ${deps.local.proxyId}
            }
        }
    }
}
