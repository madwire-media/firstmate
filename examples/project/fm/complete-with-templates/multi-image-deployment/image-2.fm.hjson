{
    kind: step/empty
    description: profile combiner for image-2

    profiles: {
        *: {
            deps: {
                image: ./default
            }

            outputs: {
                url: ${deps.image.url}
            }
        }

        local: {
            deps: {
                image: ./local
            }

            outputs: {
                url: ${deps.image.url}
                proxyId: ${deps.local.proxyId}
            }
        }
    }
}
